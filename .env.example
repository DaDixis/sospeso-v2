# Google Sheets Menu Integration
# To get your URL:
# 1. Open your Google Sheet
# 2. File > Share > Publish to web
# 3. Select the sheet tab with menu data
# 4. Choose "Comma-separated values (.csv)" format
# 5. Click Publish and copy the URL

GOOGLE_SHEET_CSV_URL=https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/pub?output=csv

# Optional: Netlify/Vercel build hook for triggering rebuilds
# This URL allows the owner to trigger a rebuild by clicking a link
BUILD_HOOK_URL=
